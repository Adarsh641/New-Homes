<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form method="POST" action="/listings/<%=listing._id%>?_method=PUT"> 
        <input type="text" value="<%= listing.title %>" name="listing[title]" placeholder="Title" required>
        <br> <br>
        <textarea name="listing[description]" required><%= listing.description %></textarea>
        <br> <br>
        <input value="<%= listing.image %>" name="listing[image]" placeholder="Image URL" >
        <br> <br>
        <input type="number" value="<%= listing.price %>" name="listing[price]" placeholder="Price" required>
        <br> <br>
        <input type="text" value="<%= listing.country %>" name="listing[country]" placeholder="Country" required >
        <br> <br>
        <input type="text" value="<%= listing.location %>" name="listing[location]" placeholder="Location" required >
        <br> <br>
        <button>Create Listing</button>

    </form>
</body>
</html>